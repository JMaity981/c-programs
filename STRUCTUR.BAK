#include<stdio.h>
#include<conio.h>
struct student
{
char name[30];
int roll;
float marks;
}st1,st2;
int main()
{
int x;
struct student st1={Ram,1,30};
struct student st2={Sam,2,40};
x=strcmp(st1.name,st2.name);
if(x==0&&st1.roll==st2.roll&&st1.marks==st2.marks)
printf("Two student are same");
getch();
return();
}
